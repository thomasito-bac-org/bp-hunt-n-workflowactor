name: sensitive-ci
on:
  workflow_dispatch:
  pull_request:
jobs:
  privileged-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "privileged job ran, secret length ${#MYSECRET}"
        env:
          MYSECRET: ${{ secrets.GITHUB_TOKEN }}
